# Project identification
sonar.projectKey=nextjs-api-inspector
sonar.projectName=nextjs api inspector
sonar.projectVersion=0.0.1

# Source code location
sonar.sources=src,webview-ui/src
sonar.tests=src,webview-ui/src

# Exclude directories
sonar.exclusions=\
  **/node_modules/**,\
  **/dist/**,\
  **/out/**,\
  **/webview-ui/build/**,\
  **/coverage/**,\
  **/__mocks__/**,\
  **/*.spec.ts,\
  **/*.spec.tsx,\
  **/*.test.ts,\
  **/*.test.tsx,\
  **/esbuild.js,\
  **/*.config.js,\
  **/*.config.ts,\
  **/vite.config.ts,\
  **/.vscode/**,\
  **/.git/**

# Test inclusions
sonar.test.inclusions=\
  **/*.test.ts,\
  **/*.test.tsx,\
  **/*.spec.ts,\
  **/*.spec.tsx

# Language
sonar.language=ts

# Encoding
sonar.sourceEncoding=UTF-8

# TypeScript specific
sonar.typescript.node=node

# Coverage report paths (if you add tests later)
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Coverage exclusions
sonar.coverage.exclusions=\
  **/__tests__/**,\
  **/__mocks__/**,\
  **/*.test.ts,\
  **/*.test.tsx,\
  **/*.spec.ts,\
  **/*.spec.tsx,\
  **/types/**,\
  **/config/**,\
  **/*.js,\
  **/*.d.ts

# Disable external issues
sonar.typescript.internal.typescriptLocation=node_modules/typescript

# Host URL
sonar.host.url=http://localhost:9000

# Authentication token (add after generating from SonarQube UI)
# Steps to generate:
# 1. Start SonarQube: npm run sonar:start
# 2. Open http://localhost:9000
# 3. Login with admin/admin (change password when prompted)
# 4. Go to My Account → Security → Generate Token
# 5. Uncomment line below and paste your token

sonar.login=sqp_0eb3822f5feb3947b2dca609fb025d2fb74b6420

